import { Title, Meta, Primary, Controls, Story, Canvas } from '@storybook/blocks';
import * as ButtonStories from '../Button/button.stories';

<Meta of={ ButtonStories } />

<Title>Button</Title>  
<Canvas sourceState="none" of={ButtonStories.Primary} ></Canvas>
NOTE: The actual Rebelmouse-specific code snippets below can only be used within the Rebelmouse web building system!  The Angular templates here are only meant for visual demonstration purposes, and the ability to copy that code will be marked unavailable for this library.  The buttons below represent various sizes that will be applied to only specific scale-based interactions determined by the size of the viewport.

## Example Implementation

### Primary

```html
<abtest id="2ff597a3-6bee-46b8-b5b4-a06148a3fef8" name="Primary" query-param="test" query-value="1" data-rm-created-by="26267932" data-rm-created-on="Mon Apr 07 2025 16:52:13 GMT-0400 (Eastern Daylight Time)" data-rm-selected="true" data-rm-ld-id="rm598">

 <component name="Button" removable="false">

  <do removable="false">

   <jinja button_text="{{button_text}}" button_link="{{button_link}}" yellow_button="{{yellow_button}}" white_button="{{white_button}}" blue_outline_button="{{blue_outline_button}}" white_outline_button="{{white_outline_button}}" chevron_link_blue="{{chevron_link_blue}}" chevron_link_blue_bold="{{chevron_link_blue_bold}}" chevron_link_white="{{chevron_link_white}}" chevron_link_white_bold="{{chevron_link_white_bold}}" data-rm-friendly-name="Button Jinja Template">

    <![CDATA[{% if node.attrs.yellow_button == "True" %}

    <a class="btn btn-primary-yellow" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.white_button == "True" %}

    <a class="btn btn-primary-white" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.blue_outline_button == "True" %}

    <a class="btn btn-secondary-blue" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.blue_outline_button == "True" %}

    <a class="btn btn-secondary-white" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.chevron_link_blue == "True" %}

    <a class="btn chevron-link-blue" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.chevron_link_blue_bold == "True" %}

    <a class="btn chevron-link-blue-bold" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.chevron_link_white == "True" %}

    <a class="btn chevron-link-white" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% elif node.attrs.chevron_link_white_bold == "True" %}

    <a class="btn chevron-link-white-bold" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% else %}

    <a class="btn btn-primary-blue" href="{{node.attrs.button_link}}">{{node.attrs.button_text}}</a>

{% endif %}]]>

   </jinja>

  </do>

  <param name="button_text" type="text" default="Button Text"/>

  <param name="button_link" type="text" default="/"/>

  <param name="yellow_button" type="bool" default="False"/>

  <param name="white_button" type="bool" default="False"/>

  <param name="blue_outline_button" type="bool" default="False"/>

  <param name="white_outline_button" type="bool" default="False"/>

  <param name="chevron_link_blue" type="bool" default="False"/>

  <param name="chevron_link_blue_bold" type="bool" default="False"/>

  <param name="chevron_link_white" type="bool" default="False"/>

  <param name="chevron_link_white_bold" type="bool" default="False"/>

 </component>

</abtest>

```